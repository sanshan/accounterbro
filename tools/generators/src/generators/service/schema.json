{
    "$schema": "https://json-schema.org/schema",
    "$id": "AccounterBroServiceGenerator",
    "title": "AccounterBro Service Generator",
    "description": "Creates a canonical internal Nest service.",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Service name in lowercase kebab-case.",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "What is the service name?"
        }
    },
    "required": ["name"]
}
