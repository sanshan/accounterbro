{
    "$schema": "https://json-schema.org/schema",
    "$id": "Package",
    "title": "AccounterBro Package",
    "description": "Create a canonical private AccounterBro package under packages/.",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Lowercase kebab-case package name without the @accounterbro scope.",
            "pattern": "^[a-z][a-z0-9-]*$",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "Package name"
        }
    },
    "required": ["name"]
}
